<UserControl x:Class="AssetManagerDesktop.RemoteFile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetManagerDesktop"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="90"
             d:DataContext="{d:DesignInstance Type=local:RemoteFile, IsDesignTimeCreatable=False}">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Rectangle Grid.ColumnSpan="2" Grid.Column="0" Fill="LightGray" RadiusX="1" RadiusY="1"/>
        <TextBlock Grid.Column="0" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="5" Text="{Binding Path=FileName, Mode=OneWay}"/>
        <Button Name="DownloadButton" Grid.Column="1"  VerticalAlignment="Center" Background="Transparent" HorizontalAlignment="Center" Content="Download" FontSize="5" BorderBrush="Transparent"/>
    </Grid>
</UserControl>
